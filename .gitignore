node_modules/
.vercel/
.project_id
.github_repo
.env
.env.*
# Android release signing (never commit the keystore)
*.jks
*.keystore
*.b64
saboo-release.jks.b64
# Release staging
dist/
RELEASE_NOTES.md
# Gradle build outputs
android/app/build/
android/.gradle/
android/build/
